| Section                | Content                                                                                                                                                                                                                    |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Course Overview**    | Objectives, marks distribution (60+25+15), passing criteria                                                                                                                                                                |
| **Unit-I**             | Python intro, installation (IDLE, Jupyter), syntax, keywords, identifiers, variables, data types, `input()`/`print()`, all 7 types of operators with tables & code                                                         |
| **Unit-II**            | `if`/`if-else`/`if-elif-else`, nested `if`, `for`/`while` loops, nested loops, `break`/`continue`/`pass`, functions (defining, parameters, return values, `*args`, `**kwargs`, lambda, recursion, scope)                   |
| **Unit-III**           | Lists (creation, slicing, methods, comprehension), Tuples (immutable, unpacking), Sets (operations, methods), Strings (indexing, methods, formatting), Dictionaries (key-value pairs, methods, nested), built-in functions |
| **Unit-IV**            | File handling (modes, read/write, `with` statement, binary, `os` module), Python libraries (pip/conda, importing), **NumPy** (arrays, operations, indexing), **Pandas** (Series, DataFrame, read/write CSV)                |
| **CO Mapping**         | CO1–CO4 mapped to all 4 units                                                                                                                                                                                              |
| **Exam Pattern**       | Section A (4×3 marks compulsory) + Section B (4×12 marks choice) = 60 marks theory                                                                                                                                         |
| **Practice Questions** | 8 questions per unit (32 total) for exam preparation                                                                                                                                                                       |
                                                                                                                                                                                |
